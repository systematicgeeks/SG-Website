export interface Testimonial {
  text: string;
  name: string;
  role: string;
}

export interface VideoProof {
  name: string;
  company: string;
  id: string; // Wistia ID
}

export interface FAQItem {
  question: string;
  answer: string;
}
